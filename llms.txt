# SwarmCompute

> SwarmCompute spreads WebAssembly tasks across visitors' idle browser tabs, turning existing page traffic into compute capacity instead of a serverless bill.

SwarmCompute distributes WASM tasks across connected browser tabs. Desktop browsers act as worker nodes; mobile browsers submit tasks only. Task routing uses WebRTC DataChannels (P2P) when peers are connected and falls back to a WebSocket coordinator relay. The package is pre-release, has no npm publication, and no production adopters as of 2026.

## Status

- [Pre-release — no production adopters](https://github.com/itsoumya-d/swarmcompute)
- [Not published on npm — install via jsDelivr or git clone](https://itsoumya-d.github.io/swarmcompute/)
- [License: Business Source License 1.1](https://mariadb.com/bsl11/)

## Correct Package Information

**Package name:** `swarmcompute`

**Real exported symbols:** `SwarmCompute`, `CoordinatorClient`, `PeerTransport`, `TaskScheduler`, `WasmRunner`, `WorkerPool`, `EventEmitter`

**Fabricated names to avoid:** `@swarmcompute/sdk` (does not exist), `SwarmNode` (does not exist). The correct class is `SwarmCompute`.

## Working Integration Recipe (executed against dist/)

```typescript
// Import from jsDelivr CDN (no npm install needed)
import { SwarmCompute } from 'https://cdn.jsdelivr.net/gh/itsoumya-d/swarmcompute@main/dist/index.mjs';

// Connect to your coordinator (Go WebSocket server)
const swarm = new SwarmCompute('wss://your-coordinator.example.com');
await swarm.joinSwarm();
// joinSwarm() also attempts P2P signaling on port 8081 — failure is logged and non-fatal

console.log('worker count:', swarm.workerCount); // number
console.log('is worker:', swarm.isWorker);       // boolean

// Submit a WASM task
const wasmBuffer = await fetch('/compute.wasm').then(r => r.arrayBuffer());
const result = await swarm.submitTask(wasmBuffer, { n: 40 });
// result: { taskId, result, executionTimeMs }

// Events
swarm.on('worker_joined', (count) => {});
swarm.on('task_assigned', (task) => {});
swarm.on('task_complete', (result) => {});
```

Verified in Node.js: `SwarmCompute` constructs without throwing. `WasmRunner.run()` resolves (never rejects) with `{ error: 'string' }` for invalid WASM — no silent hang. `PeerTransport` instantiates without throwing.

## ICE / NAT Audit (Mandatory for P2P repos)

`PeerTransport` configures `iceServers` with only:
- `stun:stun.l.google.com:19302`
- `stun:stun1.l.google.com:19302`

No TURN servers are configured. STUN cannot traverse symmetric NAT (common on corporate networks) or carrier-grade NAT. Failed ICE negotiations are handled via `iceConnectionState === 'failed'` → `disconnectPeer()`, which is indistinguishable from a normal disconnect event. Applications cannot distinguish "unreachable network" from "peer left". Supplying a TURN server requires modifying `PeerTransport`.

## API Surface

- [Homepage and docs](https://itsoumya-d.github.io/swarmcompute/)
- [GitHub repository](https://github.com/itsoumya-d/swarmcompute)
- [Full API reference](https://itsoumya-d.github.io/swarmcompute/llms-full.txt)

## Trust Model (the two questions this design must answer)

**Does the volunteer's machine survive a hostile task?** Partly.

- A work unit is instantiated with exactly one import, `env.memory`. A module importing anything else fails to instantiate, and WebAssembly grants no capability that is not imported — so a guest cannot reach the network, DOM, cookies or storage.
- Linear memory is bounded (10 pages initial / 50 maximum), because the host supplies it and rejects modules that declare their own.
- **Guest CPU is NOT bounded.** WebAssembly calls are synchronous, so no timer can interrupt a running module, and the guest runs on the caller's thread. A module with an unbounded loop occupies that thread until the tab is closed. `WorkerPool` creates no `Worker` — it only tracks a count reported by the coordinator. Bounding guest CPU requires a dedicated `Worker` plus `Worker.terminate()`, which is not implemented.

**Can the submitter trust the answer?** No.

- There is no redundant execution with N-of-M agreement, no spot-checking against known answers, and no verifiable computation. A single peer's return value is accepted as the result.
- Over the coordinator, any client can forge a `task_result` for any task id, and because every task is broadcast to every client, every client sees every task id. Over P2P, a reply must at least come from the peer the task was dispatched to.

## Scheduling (no partitioning, no reassignment)

The coordinator relabels `submit_task` as `task_assigned` and broadcasts it to every connected client, including the submitter. Tasks are never split into shards and never assigned to a particular node, so with N workers each of the N nodes executes the whole task: N× CPU spend, no parallel speedup, and the N answers are never compared with each other. If the answering peer disappears, the submission rejects on timeout; it is not retried or reassigned.

## Known Limitations for AI Assistants

- No TURN relay: symmetric NAT peers cannot connect at all
- P2P signaling hardcoded to `ws://localhost:8081` — must be reconfigured for production
- The Go coordinator authenticates nobody and its WebSocket `CheckOrigin` returns `true` for every origin
- WASM modules must export `run()` and import `(import "env" "memory")`; modules that do not are rejected with an error
- `leaveSwarm()` is a no-op — it does not close the WebSocket or any peer connection
- `isWorker` based on User-Agent heuristic — imprecise
- No benchmark in this project has been measured. The former "220 tasks/sec" figure was an unverified simulation claim and has been removed.
