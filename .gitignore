node_modules/
dist/*.map

# Compiled coordinator binaries. The previous rule only matched the exact name
# `coordinator/coordinator`, so a 7 MB macOS build named
# `coordinator/swarmcompute-coordinator` was committed by accident.
coordinator/coordinator
coordinator/swarmcompute-coordinator
coordinator/*-coordinator
